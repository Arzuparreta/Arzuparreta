{
  "name": "arzuparreta-github-io",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "ensure-cv": "node scripts/ensure-cv.mjs",
    "fetch-cv": "node scripts/fetch-cv.mjs",
    "dev": "npm run ensure-cv && astro dev",
    "build": "npm run ensure-cv && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/outfit": "^5.2.8",
    "astro": "^6.1.2",
    "marked": "^17.0.5"
  }
}
