# Platform Layer

Cross-project assets live here. Keep this layer generic.

- `ci/`: reusable CI snippets or shared validation scripts.
- `templates/`: starter templates for new project profiles.
- `docs-kit/`: reusable docs fragments and scaffolds.
- `devtools/`: repo-level helper scripts.

This folder is intentionally profile-driven, not stack-locked.
